import React from 'react';

const League = (league) => {
	return (
		<div className='league'>
			<h1>Add New League</h1>
			{/* <form onSubmit={league.handleSubmit}>
				<label>League Name:</label>
				<input type='text' name='name' onChange={league.handleChange} />
				<label>Cost to Sponsor:</label>
				<input type='number' name='cost' onChange={league.handleChange} />
			</form> */}
		</div>
	);
};

export default League;
